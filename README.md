# GitHub Actions Test Project

This project demonstrates GitHub Actions with Docker Compose on Windows.

## Features
- Docker Compose with Nginx and PostgreSQL
- GitHub Actions workflow
- Self-hosted runner on Windows

## How to run
```bash
docker-compose up -d
The workflow automatically tests the deployment when code is pushed to main branch.
