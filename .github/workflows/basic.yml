name: Basic Test
on: [workflow_dispatch]
jobs:
  basic:
    runs-on: self-hosted
    steps:
      - run: echo "Hello from Basic Test!"
      - run: echo "This should definitely work"
      - run: echo "Creating final success marker..."
      - run: echo "SUCCESS" > success.txt
      - uses: actions/upload-artifact@v4
        with:
          name: basic-artifacts
          path: success.txt
